---
"@juggling-tools/simulator-react": minor
---

Add `foreground` prop to `Simulator.Root` for customising juggler and hand stroke colour. Support transparent backgrounds. Re-export `DEFAULT_FOREGROUND` from core.
